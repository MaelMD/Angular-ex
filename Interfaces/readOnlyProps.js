"use strict";
// interface Circle {
//     readonly centerX : number;
//     readonly centerY: number;
//     readonly radius: number;
// }
function moveCircle(c, centerX, centerY, radius) {
    c.centerX = centerX;
    c.centerY = centerY;
    c.radius = radius;
}
